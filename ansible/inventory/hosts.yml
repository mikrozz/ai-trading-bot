all:
  children:
    trading:
      hosts:
        trading01:
          ansible_host: 127.0.0.1
          ansible_connection: local
      vars:
        trading_bot_root: /opt/ai-trading-bot
        trading_bot_user: root
        trading_secrets_src: /root/.config/trading-bot/binance_testnet.env
        trading_compose_services:
          - redis
          - timescaledb
